---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sulmoon-auth
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sulmoon
  template:
    metadata:
      labels:
        app: sulmoon
    spec:
      serviceAccountName: aws-secrets
      containers:
      - image: sulmoon/auth-svc:162
        name: auth-svc
        volumeMounts:
        - name: aws-secrets
          mountPath: "/secret/goormprj"
          readOnly: true
      volumes:
      - name: aws-secrets
        csi:
          driver: secrets-store.csi.k8s.io
          readOnly: true
          volumeAttributes:
            secretProviderClass: "aws-secrets"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sulmoon-survey
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sulmoon
  template:
    metadata:
      labels:
        app: sulmoon
    spec:
      serviceAccountName: aws-secrets
      containers:
      - image: sulmoon/survey-svc:162
        name: survey-svc
        volumeMounts:
        - name: aws-secrets
          mountPath: "/secret/goormprj"
          readOnly: true
      volumes:
      - name: aws-secrets
        csi:
          driver: secrets-store.csi.k8s.io
          readOnly: true
          volumeAttributes:
            secretProviderClass: "aws-secrets"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sulmoon-user
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sulmoon
  template:
    metadata:
      labels:
        app: sulmoon
    spec:
      serviceAccountName: aws-secrets
      containers:
      - image: sulmoon/user-svc:162
        name: user-svc
        volumeMounts:
        - name: aws-secrets
          mountPath: "/secret/goormprj"
          readOnly: true
      volumes:
      - name: aws-secrets
        csi:
          driver: secrets-store.csi.k8s.io
          readOnly: true
          volumeAttributes:
            secretProviderClass: "aws-secrets"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sulmoon-api-gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sulmoon-gate
  template:
    metadata:
      labels:
        app: sulmoon-gate
    spec:
      containers:
      - image: sulmoon/api-gateway-svc:162
        name: api-gateway
        ports:
        - containerPort: 10000
      nodeSelector:
        node: svc
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sulmoon-discovery
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sulmoon
  template:
    metadata:
      labels:
        app: sulmoon
    spec:
      containers:
      - image: sulmoon/discovery-svc:162
        name: discovery-svc
        ports:
        - containerPort: 8761
      nodeSelector:
        node: svc
